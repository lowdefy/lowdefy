// A file-plugin connection: the connection object a package connection exports,
// minus the barrel. Its schema is declared in StaticRows.json and the build
// assembles its requests map from the files under requests/, so there is
// nothing left for the connection file itself to declare.
export default {};
