---
'@lowdefy/docs': patch
'@lowdefy/docs-content': patch
---

docs: Document journey targets (`row`, `column`, `text`, `nth`) for grid cell buttons, confirm dialogs and menu items on the config tests and AI agent docs pages.
