---
'@lowdefy/docs': patch
'@lowdefy/docs-content': patch
---

docs: Document `lowdefy_run_journey` / `POST /lowdefy-docs/journey` on the AI agent docs page — the step grammar, the failure shape and a full example.
