---
'@lowdefy/blocks-antd': major
---

Rename `Segmented` block to `SegmentedSelector` to follow the selector naming convention. Now uses the standard selector value flow, supporting complex option values and HTML option labels.

**Migration:** Replace `type: Segmented` with `type: SegmentedSelector` in your config.
