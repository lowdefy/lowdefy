---
'@lowdefy/blocks-antd': patch
---

Fix e2e tests for antd v6. Update class name selectors (e.g. `.ant-input-number-handler-*` → `.ant-input-number-action-*`), fix Notification placement assertions, update TimelineList test data to match block IDs, and adjust interaction patterns for antd v6 InputNumber behavior.
