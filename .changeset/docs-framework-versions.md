---
'@lowdefy/docs': patch
---

docs: Add a "Framework versions" page under Deployment explaining the `latest`, `known-good` and `experimental` npm channels, recommending `lowdefy: known-good` for apps tracking pre-release work, describing what the canary validates (`build`, `check`, `test`), and noting that a broken `known-good` build is a framework bug worth reporting.
