---
'@lowdefy/blocks-antd': major
---

Replace `ColorPicker` with `ColorSelector` using antd's native ColorPicker component. The separate `@lowdefy/blocks-color-selectors` package has been removed — `ColorSelector` is now in `@lowdefy/blocks-antd`.
