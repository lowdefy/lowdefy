---
'@lowdefy/blocks-aggrid': patch
---

feat(aggrid): buttons cells get tooltips. An icon-only button (`hideTitle: true`) now shows its `title` on hover, so a row of glyphs says what each one does; a `tooltip` key on any button sets its own hover text.
