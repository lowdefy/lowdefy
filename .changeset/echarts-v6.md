---
'@lowdefy/blocks-echarts': minor
---

Upgrade echarts to v6.0.0 and echarts-for-react to v3.0.5. Rework chart sizing to use a wrapper div with `minWidth: 0` for proper flex container behavior, with inner chart at `width: 100%; height: 100%` for responsive fill.
