---
'@lowdefy/blocks-antd': major
---

Rename Tabs `tabPosition` property to `tabPlacement` to match antd v6 API.

**Migration:** Replace `tabPosition` with `tabPlacement` (`top`, `bottom`, `left`, `right`).
