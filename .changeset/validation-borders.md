---
'@lowdefy/blocks-antd': patch
---

Add validation error borders to input blocks. TextArea and AutoComplete now pass `status` for antd validation styling. Group-based inputs (Radio, Checkbox, ButtonSelector, SegmentedSelector) show CSS-based error/warning borders via the Label wrapper.
