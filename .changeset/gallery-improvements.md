---
'@lowdefy/blocks-antd': patch
---

Improve block gallery examples. Avatar Comment Thread now uses inline chat-style layout. FloatButton gallery rewritten to use a single floating button with trigger buttons per section instead of many forced-relative buttons. Divider gallery updated for new `orientation`/`titlePlacement` properties.
