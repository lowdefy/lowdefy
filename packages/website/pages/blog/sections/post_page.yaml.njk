{% set blog_root = 'pages/blog/categories' %}
{% set components = 'pages/blog/components/post_page' %}
{% set post_path = [blog_root, post.category, post.slug] | join("/") %}
{% set content_path = [post_path, "content.md"] | join("/") %}
{% set post_sections = ["post_header", "post_content", "post_footer"] %}

id: post_page
type: Box
style:
  maxWidth: 800px
  margin: 60px auto
layout:
  contentJustify: center
blocks:
{% for section in post_sections %}
  - _ref: 
      path: {{ [components, section] | join("/") + ".yaml.njk" }}
      vars: 
        _ref: {{ [post_path, "vars"] | join("/") + ".yaml" }}
        content_path: {{ content_path }}
{% endfor %}

