{
  "type": "object",
  "properties": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "options": {
        "type": "array",
        "description": "Options for the segmented control. Array of strings or { label, value, icon, disabled } objects."
      },
      "size": {
        "type": "string",
        "enum": ["small", "middle", "large"],
        "description": "Size of the segmented control."
      },
      "block": {
        "type": "boolean",
        "default": false,
        "description": "Option to fit width to its parent's width."
      },
      "disabled": {
        "type": "boolean",
        "default": false,
        "description": "Disable the segmented control."
      },
      "theme": {
        "type": "object",
        "description": "Antd design token overrides for this block. See <a href=\"https://ant.design/components/overview#design-token\">antd design tokens</a>.",
        "docs": {
          "displayType": "yaml"
        }
      }
    }
  },
  "cssKeys": ["element"]
}
