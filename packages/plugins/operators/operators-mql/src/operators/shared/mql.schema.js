export default {
  type: 'object',
  params: {
    description:
      'MQL method params. Accepts array positional args or object with named args depending on method (aggregate, expr, test).',
  },
};
