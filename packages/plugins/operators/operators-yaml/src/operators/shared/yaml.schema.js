export default {
  type: 'object',
  params: {
    description:
      'YAML method params. For parse: object/array with named args (on, options). For stringify: object/array with named args (on, options).',
  },
};
