export default {
  type: 'object',
  params: {
    type: 'array',
    minItems: 2,
    maxItems: 2,
    description: 'Array of two values. Returns true if the first is less than the second.',
  },
};
