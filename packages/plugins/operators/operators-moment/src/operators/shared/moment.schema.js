export default {
  type: 'object',
  params: {
    description:
      'Moment method params. Accepts array positional args or object with named args (on, locale, format/withSuffix/thresholds).',
  },
};
