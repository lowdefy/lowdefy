# A2: Missing Connection ID
# ==========================
# Tests that a connection without an id throws an error with location.
#
# Expected error:
#   [Config Error] Connection id missing.
#
# The error points to line 13 where the connection without id is defined.

lowdefy: local

connections:
  - type: AxiosHttp
    properties:
      baseURL: http://example.com

pages:
  - id: home
    type: Box
