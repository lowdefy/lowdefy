# O8: Module uses _module.connectionId referencing a non-existent connection
# Tests that the build throws when _module.connectionId references a connection the module doesn't export.
lowdefy: local

modules:
  - id: widget
    source: 'file:modules/widget'
    vars: {}

pages:
  - id: home
    type: Box
