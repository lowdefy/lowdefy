# OA: Module source directory has no module.yaml
# Tests that the build throws when the file: source points to a directory without module.yaml.
lowdefy: local

modules:
  - id: empty
    source: 'file:modules/empty'
    vars: {}

pages:
  - id: home
    type: Box
