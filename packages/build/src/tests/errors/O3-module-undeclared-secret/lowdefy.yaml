# O3: Module references a secret not in its whitelist
# Tests that the build throws when a module uses _secret for an undeclared secret name.
lowdefy: local

modules:
  - id: leaky
    source: 'file:modules/leaky'
    vars: {}

pages:
  - id: home
    type: Box
