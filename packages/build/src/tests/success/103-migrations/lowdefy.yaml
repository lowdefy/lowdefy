# 103: Migrations discovered from the migrations/ directory
# ========================================================
# Two migration files under migrations/ are discovered, validated through the
# same buildRoutine code an Api endpoint uses, and written one artifact per
# migration to build/migrations/<id>.json plus the ordered index
# build/migrations.json. The ledger connection is named at config.migrations.

lowdefy: local

config:
  migrations:
    ledgerConnectionId: migrations
    preflight: true

connections:
  - id: frameworks
    type: MongoDBCollection
    properties:
      databaseUri: mongodb://localhost/test
      collection: frameworks
      write: true
  - id: migrations
    type: MongoDBCollection
    properties:
      databaseUri: mongodb://localhost/test
      collection: migrations
      write: true

pages:
  - id: home
    type: Box
    blocks:
      - id: title
        type: Paragraph
        properties:
          content: Migrations
