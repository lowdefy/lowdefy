id: chat
type: Box
properties:
  # Mimics AgentChat.agentId — a block property must carry the scoped id.
  content:
    _module.agentId: assistant
blocks:
  - id: title
    type: Title
    properties:
      content: Assistant Chat
