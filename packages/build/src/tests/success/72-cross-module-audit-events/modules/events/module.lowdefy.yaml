name: Events
description: Event logging module

exports:
  api:
    - id: log-event

connections:
  - id: event-store
    type: AxiosHttp
    properties:
      baseURL: http://localhost:8080

api:
  - _ref: api/log-event.yaml
